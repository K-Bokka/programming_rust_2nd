pub fn run() -> Result<(), Box<dyn std::error::Error>> {
    println!("22.1 What is unsafe about it?");

    // let mut a: usize = 0;
    // let ptr = &mut a as *mut usize;
    // unsafe {
    //     *ptr.offset(3) = 0x7ffff72f484c;
    // }

    Ok(())
}
